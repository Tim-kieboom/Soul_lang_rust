main(str[] args) int {
    
    if args.Len() < 2 {
        Println("error file not given");
        return 1;
    }

    const filePath = args[1]
    const file = Open(filePath)

    Println(file.Lines().Select((index, line) => f"{index}. {words}"))
}



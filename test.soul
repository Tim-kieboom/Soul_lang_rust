const ch = 'a'
const number = 1
Literal gravity = 9.81
const f32 float = 1
Literal uint uintNumber = 1

const strarray = ["ello", "hoiu", "werg", "werg"]

// wrong() {
// 	mutNum := 1
// 	// const d = mutNum + 2
// 	return
// }

// const T[] Len<T>(this@) uint {
// 	return __soul_internal_length__(this)
// }

// type string typeof str

array() {
	array := [1,2,3,4]
	second := array[1]
	// array[0] = 200
	Println(array[0])
}

whileArgs(str[]@ args) {
	argsLen := __soul_internal_length__(args)
	// i := 0
	// while i++ < argsLen {
	// 	Println("{i}.{args[i]}")
	// }	
}

ref() {
	f := @1
	num := i32(1)
	i32@ ref = @num
	buffer := str()
	str@ refstr = @buffer
}

printInput() {
	buffer := str()

	Print("foo ")
	Input(&buffer)
	Println(@buffer)
}

main(str[]@ args) int {
	sum(int a, int b) int {
		return a + b
	}
	Println(args[0])
	Println(f"hello world")
	Println(f"1 + 2 = {sum(1, 2)}")

	array()
	// whileArgs(args)
	// ref()
	// printInput()

	return 0
}

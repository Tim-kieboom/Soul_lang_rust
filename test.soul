const ch = 'a'
const number = 1
const f32 float = 1
Literal gravity = 9.81
Literal uint uintNumber = 1

const strarray = ["ello", "hoiu", "werg", "werg"]

// const T[] Len<T>(this@) uint {
// 	return __soul_internal_length__(this)
// }

// wrong() {
// 	var := int(1) - 1
// }

// forIntArrays(int[]@ arr) {
// 	len := __soul_internal_length__(arr)
// 	for i in 0:len {
// 		Println(f"{i}.{arr[i]}")
// 	}
// }

struct Data {
	i32 interger = 0
	str string = ""
}

class IntList {
	int[] buffer 
	int len

	Len(this@) uint {
		return len
	}
}

ifs() {
	condition := 1
	if condition == 1 {
		int f = 1
		condition = 1
		Println(condition)

	}
	else if condition == 2 {
		int f = 1
		Println(condition)
	}
	else {
		int f = 1
		Println(condition)
	}
}

array() {
	array := [1,2,3,4]
	uint index = 3
	string name = 1
	second := @array[1]
	second = @array[index]
	array[0] = 200
	Println(array[0])
}

whileArgs(str[]@ args) {
	argsLen := __soul_internal_length__(args)
	i := -1
	lastIndex := int(argsLen); lastIndex -= 1
	while i++ < lastIndex {
		Println(f"{i}.{args[uint(i)]}")
	}
}

ref() {
	f := @1
	num := i32(1)
	i32@ ref = @num
	buffer := str()
	str@ refstr = @buffer
}

Input(mut str& buffer, str prefix = "") {
	Print(prefix)
	Input(buffer)
}

main(str[]@ args) int {
	sum(int a, int b) int {
		return a + b
	}

	Println(args[0])
	Println("hello world")
	Println(f"1 + 2 = {sum(1, 2)}")

	array()
	whileArgs(args)
	ref()

	buffer := str()
	Input(&buffer, prefix: "input: ")
	Println(buffer)

	ifs()

	EPrintln("!!error!! test error")

	return 0
}





